=render 'form'

%table#tasks
  %thead
    %tr
      %th{:colspan => '2'} Task
  %tbody
    =render @tasks

- content_for :js do
  setupTasksIndex("#{reorder_tasks_path}", "#{tasks_path}");
  setupTaskMenus();