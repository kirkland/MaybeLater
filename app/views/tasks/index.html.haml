%h1 Tasks

=render 'form'

%table#tasks
  %tr
    %th Task
  -@tasks.each do |t|
    %tr{:class => cycle('even','odd'), 'data-id' => t.id}
      %td=t.content

- content_for :js do
  setupTasksIndex("#{reorder_tasks_path}", "#{tasks_path}");
